const initState = {
    redirectTo: "",
    isAuth: false,
    msg: "",
    user: "",
    type: ""
};
export function user(state = initState, action) {
    switch (action.type) {
        default:
            return state;
    }
}
